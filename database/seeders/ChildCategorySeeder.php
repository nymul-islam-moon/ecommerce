<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class ChildCategorySeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // Use the childcategory model and make a seeder for it with appropriate data.
        // This is a placeholder for the actual seeding logic.
        // You can use factories or manually create child categories here.
        // Example:
        // ChildCategory::factory()->count(10)->create();

    }
}
